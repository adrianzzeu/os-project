# AI Usage

AI assistance was used to plan and implement the command-line role checks and
the filter parser shape. The implementation was verified locally with compiler
warnings enabled and by exercising the `--add`, `--list`, `--show`,
`--metadata`, and `--remove_report` commands.
